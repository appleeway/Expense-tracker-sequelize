{{!-- views/newedit.handlebars --}}
<div class="mt-5">
  <div class="row">
    <div class="col-md-6 mx-auto text-center">
      <h4>New Expense</h4>

      {{!-- 依不同頁面執行不同動作 --}}
      {{#ifEquals action '修改'}}
      <form action="/records/{{{record.id}}}?_method=PUT" method="POST">
        {{else}}
        <form action="/records/new" method="POST">
          {{/ifEquals}}
          <div class="form-group row">
            <label for="name" class="col-sm-2 col-form-label">Name</label>
            <div class="col-sm-10">
              <input type="text" class="form-control" id="name" name="name" placeholder="" value="{{record.name}}"
                required>
            </div>
          </div>
          <div class="form-group row">
            <label for="date" class="col-sm-2 col-form-label">Date</label>
            <div class="col-sm-10">
              <input type="date" class="form-control" id="date" name="date" placeholder="" value="{{record.date}}"
                required>
            </div>
          </div>
          <div class="form-group row">
            <label for="amount" class="col-sm-2 col-form-label">Amount</label>
            <div class="col-sm-10">
              <input type="text" class="form-control" id="amount" name="amount" placeholder="" value="{{record.amount}}"
                required>
            </div>
          </div>
          <fieldset class="form-group">
            <div class="row">
              <legend class="col-form-label col-sm-2 pt-0">Category</legend>
              <div class="col-sm-10 text-left">
                <div class="form-check">
                  <input class="form-check-input" type="radio" name="category" id="gridRadios1" value="Housing"
                    {{#ifEquals record.category "Housing"}} checked{{/ifEquals}}>
                  <label class="form-check-label" for="gridRadios1">
                    Housing
                  </label>
                </div>
                <div class="form-check">
                  <input class="form-check-input" type="radio" name="category" id="gridRadios2" value="Transportation"
                    {{#ifEquals record.category "Transportation"}} checked{{/ifEquals}}>
                  <label class="form-check-label" for="gridRadios2">
                    Transportation
                  </label>
                </div>
                <div class="form-check disabled">
                  <input class="form-check-input" type="radio" name="category" id="gridRadios3" value="Entertainment"
                    {{#ifEquals record.category "Entertainment"}} checked{{/ifEquals}}>
                  <label class="form-check-label" for="gridRadios3">
                    Entertainment
                  </label>
                </div>
                <div class="form-check disabled">
                  <input class="form-check-input" type="radio" name="category" id="gridRadios4" value="Food"
                    {{#ifEquals record.category "Food"}} checked{{/ifEquals}}>
                  <label class="form-check-label" for="gridRadios4">
                    Food
                  </label>
                </div>
                <div class="form-check disabled">
                  <input class="form-check-input" type="radio" name="category" id="gridRadios5" value="Others"
                    {{#ifEquals record.category "Others"}} checked{{/ifEquals}}>
                  <label class="form-check-label" for="gridRadios5">
                    Others
                  </label>
                </div>
              </div>
            </div>
          </fieldset>
          <div class="form-group row">
            <div class="col-sm-10 mx-auto">
              {{!-- 依不同頁面執行不同動作 --}}
              {{#ifEquals action '修改'}}
              <button type="submit" class="btn btn-outline-dark mt-3">Edit Expense</a>
                {{else}}
                <button type="submit" class="btn btn-outline-dark mt-3">Submit Expense</a>
                  {{/ifEquals}}
            </div>
          </div>
        </form>
    </div>
  </div>
</div>